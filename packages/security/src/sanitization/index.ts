/**
 * Sanitization utilities
 *
 * Re-export validation utilities for backward compatibility
 * In future, this could include more specialized sanitization functions
 */

export { InputValidator } from '../validation/InputValidator';
